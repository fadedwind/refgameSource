[gd_resource type="ShaderMaterial" load_steps=3 format=2]

[ext_resource path="res://Assets/Noise/BubbleNoise.png" type="Texture" id=1]

[sub_resource type="Shader" id=195]
code = "shader_type canvas_item;

//uniform float dissolveState : hint_range(0.0, 1.0);
uniform sampler2D noise;

void fragment(){
	COLOR *= texture(TEXTURE, UV);
	COLOR.a *= step(texture(noise, UV).r, MODULATE.a);
	COLOR.rgb *= MODULATE.r;
}"

[resource]
shader = SubResource( 195 )
shader_param/noise = ExtResource( 1 )
